# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="postgresql://postgres:postgres@momentum-db:5432/momentum?schema=public"

# Clerk Authentication
# To set up Clerk:
# 1. Create an account at https://clerk.dev/
# 2. Create a new application
# 3. Get your API keys from the Clerk dashboard
# 4. Replace the placeholder values below with your actual keys
# IMPORTANT: Your publishable key should start with 'pk_test_' or 'pk_live_'
# Your secret key should start with 'sk_test_' or 'sk_live_'
# These are mock keys for development only - they will pass Clerk's validation but won't work for real auth
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_ZXhwZXJ0LWNyYXlmaXNoLTk3LmNsZXJrLmFjY291bnRzLmRldiQ
CLERK_SECRET_KEY=sk_test_H3luKwWppPY4hTP2BSfd59j50kQoSh5pjUU7Pr67nk
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# OpenAI
# Get your API key from https://platform.openai.com/api-keys
# NOTE: The current API key has exceeded its quota. To fix this:
# 1. Create a new API key in your OpenAI account
# 2. Make sure your account has available credits or is on a paid plan
# 3. Replace the key below with your new API key
OPENAI_API_KEY=your_openai_api_key_here

# Ollama (Open Source AI Model)
# If you're running Ollama locally outside Docker, use host.docker.internal to access it from inside Docker
OLLAMA_API_URL=http://host.docker.internal:11434
# The model to use with Ollama (must be installed on your Ollama server)
OLLAMA_MODEL=llama3
# Set to OLLAMA to use open source models by default, or OPENAI to use OpenAI
DEFAULT_AI_PROVIDER=OLLAMA

# Stripe
STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key 